# Префикс Docker-реестра. Замените на ваш GitHub username.
# Пример: ghcr.io/nugumanov
REGISTRY_PREFIX=ghcr.io/YOUR_GITHUB_USERNAME

# Credentials для PostgreSQL
DB_USERNAME=pricetracker
DB_PASSWORD=change_me_to_a_strong_password

# Telegram Bot Token хранится в Consul KV, а не здесь.
# После первого `docker compose up -d` выполните на сервере:
#   docker exec consul consul kv put config/price-notifier/data \
#     "$(printf 'telegram:\n  bot:\n    token: ВАШ_ТОКЕН')"
